"""End-of-day workflow: session-flatten reconciliation, journal/digest generation, and the nightly performance + guardrail handoff (MASTER_PLAN.md §4)."""

# TODO: implement EOD sequence (flatten check, reconcile, journal, EOD digest).
