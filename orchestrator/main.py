"""CLI entry point and long-running orchestrator loop that wires the event bus, workflows, and fast loop (MASTER_PLAN.md §4)."""

# TODO: implement argument parsing and the long-running asyncio loop; reconcile (orderbook/reconcile.py) must run FIRST on boot.
