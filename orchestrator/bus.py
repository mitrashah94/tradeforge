"""Asyncio event bus backed by a persisted, replayable DuckDB ``events`` table — the event-driven core (MASTER_PLAN.md §4)."""

# TODO: implement async pub/sub plus durable append/replay of events; do NOT import duckdb at module top level in this stub.
