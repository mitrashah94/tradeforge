"""Backtest engine: vectorbt/backtesting.py-based simulation with a realistic small-account cost model (commission + spread + slippage) (MASTER_PLAN.md §4, §5)."""

# TODO: implement the simulation loop; do NOT import vectorbt/pandas at module top level in this stub.
